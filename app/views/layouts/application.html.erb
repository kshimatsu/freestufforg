<!DOCTYPE html>
<html>
<head>
  <title>Freestufforg</title>
  <%= stylesheet_link_tag '//maxcdn.bootstrapcdn.com/bootstrap/3.3.0/css/bootstrap.min.css' %>
  <%= stylesheet_link_tag '//fonts.googleapis.com/css?family=Lato:100,400,100italic,400italic,900italic' %>
  <%= stylesheet_link_tag    'application', media: 'all'%>
  <%= csrf_meta_tags %>
  </head>

  <body>
  <div ng-app="freeItems" ng-controller="ListController">

    <div id="welcome" ng-hide="welcome_hide">
      <div class="welcome-message">
        <h1>Welcome to Free Stuff</h1>
        <h2>Free stuff. Either give or take.</h2>
        <p>Live to give. Share to care. Your choice!</p>
        <button class="btn btn-info" ng-click="welcome_hide = true">Get Started</button>
      </div>
    </div>

    <nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="#" id="projectname">freestuff.org</a>
        </div>
        <div id="navbar" class="collapse navbar-collapse">
          <ul class="nav navbar-nav">
            <li id="list-item-btn"><a href="#" ng-click="resetItemModal()" data-toggle="modal" data-target="#myModal">Give a Free Item</a></li>

          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">Select Location <span class="caret"></span></a>
            <ul class="dropdown-menu" role="menu">
              <li ng-click="resetLocations()"><a href="#">All</a></li>
              <li ng-repeat="location in locations"><a href="#" ng-click="filterLocation(location)">{{location}}</a></li>
            </ul>
          </li>
          </ul>
      </div>
    </nav>
    <div class="container">

      <%= yield %>

    </div>

  <%= javascript_include_tag 'application' %>
  <%= javascript_include_tag '//maxcdn.bootstrapcdn.com/bootstrap/3.3.0/js/bootstrap.min.js' %>
  <% if Rails.env == "production" %>
    <script>
    (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
    (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
    m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
    })(window,document,'script','//www.google-analytics.com/analytics.js','ga');

    ga('create', 'UA-57147868-1', 'auto');
    ga('send', 'pageview');

  </script>
<% end %>
</div>
</body>
</html>


