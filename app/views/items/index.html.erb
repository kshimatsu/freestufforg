<div class="row" id="mainContentContainer">
  <div class="col-md-12" id="sub-main-content-container">

    <div class="row">
      <div class="col-md-6">
        <div class="row">
          <div ng-repeat="item in itemList | filter:location" class="col-md-6">
            <div class="thumbnail" ng-click="updateCurrentItem(item)" ng-mouseenter="visible=true" ng-mouseleave="visible=false">
              <img class="list-image" ng-src="{{ item.media[0].file_name.url }}" />

              <div ng-show="visible">
                <div  class="thumbnail-description">
                  <h2>{{ item.remaining_days }}</h2><br/>

                  <button class="btn btn-default" id="get-this-item-btn" type="button" data-toggle="modal" data-target="#contactSeller" ng-click="getContact(item)">Contact the Lister</button>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- Second half of page -->
      <div class="col-sm-6" id="itemDescriptionContainer">
        <div class="col-sm-10 light inner-container-description" ng-hide="showItemDesc" id="hidden-div">
          <h1>Welcome to Free Stuff</h1>
          <h2 id="info-tag">Let someone else do the heavy lifting!</h2>
          <p>Free Stuff makes it quick and easy for you to post unwanted items and arrange for others to take it away for you.</p>
          <p>To get started, list an item by clicking the top or scroll through items on the left to find a treasure</p>
          <br>
          <h2>Why should I list my items?</h2>
            <p>Reduce waste by giving away things you don't need</p>
            <p>It's quick, easy and free!</p>



        </div>

        <div ng-show="showItemDesc">

          <div class="row" id="descriptionContainer">
            <div class="col-sm-9" id="title-in-desc"><h1>{{ currentItem.title }}</h1></div>
            <div class="col-sm-3">
              <div class="btn btn-default" type="button" data-toggle="modal" data-target="#contactSeller" ng-click="getContact(currentItem)">Get This Item</div>
            </div>
          </div>



          <div class="row">
            <div class="col-sm-12 sub-location">
              <h3>Located in {{ currentItem.location }} with {{currentItem.remaining_days}}</h3>
            </div>
          </div>

          <div class="row">
            <div class="col-sm-12 sub-description">
              {{ currentItem.description }}
            </div>
          </div>

          <div class="row">
            <div class="col-sm-12 sub-photo">
              <br><br>
              <div class="row">
                <div ng-repeat="medium in currentItem.media">
                  <div class="col-sm-4">
                    <img class="description-image" id="multi-images" ng-src="{{ medium.file_name.url }}" />
                  </div>
                </div>
              </div>
            </div>
          </div>

        </div>
      </div>


      <%= render 'create_item_modal' %>
      <%= render 'contact_seller_modal' %>

    </div>

  </div>
</div>


